# In-memory H2 for tests (no local MySQL required). Profile: test
spring:
  datasource:
    url: jdbc:h2:mem:employee_management;DB_CLOSE_DELAY=-1;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
  jpa:
    hibernate:
      ddl-auto: create-drop
